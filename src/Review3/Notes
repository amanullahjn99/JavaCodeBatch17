Logical operators:
    Logical operators helo us combine multipe boolean values.
    There are three(||)   3) AND (&&)
1)NOT(!)
    boolean cond=ture;
    sout"!cond"
2)OR (||)
    if one is true all of them get true. unless all of them get false.

3)AND (&&)
    if

Switch:
    it is not using in all data type like boolean, realational operator(<>=),
    logical operators(!,&&,||), double, long...
    but it has some benefite.
Loop: